<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color for the message bubble -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="8dp" /> <!-- Customize corner radius as needed -->
            <solid android:color="@color/white" /> <!-- Customize the background color -->
        </shape>
    </item>
    <!-- Border for the message bubble (black) -->
    <item android:right="1dp" android:left="1dp" android:top="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <stroke android:width="1dp" android:color="#000000" /> <!-- Border color: black, Border width: 1dp -->
        </shape>
    </item>
</layer-list>